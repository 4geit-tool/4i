.template 1
.for project.module as m where !defined(m.virtual)
.
.cached = cache_open("$(m.name)_command.sh")
#!/bin/bash

.copyleft()

pushd $(global.build_path:)

.if defined(project.python_version)
virtualenv$(project.python_version) .
source bin/activate
.endif

pushd $(m.name)

$*

popd

.if defined(project.python_version)
deactivate
.endif

popd
.
.cache_close(cached)
.chmod('+x', '$(m.name)_command.sh')
.
.endfor
.endtemplate