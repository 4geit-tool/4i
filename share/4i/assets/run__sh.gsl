.template 1
.cached = cache_open("run.sh", "", global.build_path, global.cache_path)
#!/bin/bash

.copyleft()

pushd $(global.build_path:)

.if defined(project.python_version)
virtualenv$(project.python_version) .
source bin/activate
.endif

pushd $1

\./run.sh

popd

.if defined(project.python_version)
deactivate
.endif

popd
.
.cache_close(cached, global.build_path, global.cache_path)
.chmod('+x', '$(global.build_path:)/run.sh')
.
.endtemplate