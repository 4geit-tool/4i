.template 0

if defined(projectnode.parent()) & name(projectnode.parent()) = 'dir'
  parentdir = projectnode.parent()
  if defined(parentdir.path)
    projectnode.path = parentdir.path + '/' + projectnode.name
  endif
else
  projectnode.path = projectnode.name
endif

.endtemplate